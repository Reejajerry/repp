#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("Hello, World!");
   printf("just an editing");
   printf("change in local");
   printf("changing");
   return 0;
}
